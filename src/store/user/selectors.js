export const selectToken = (state) => state.user.token;

export const selectUser = (state) => state.user;

export const selectUserLookingAt = (state) => state.user.lookingAt;

export const selectUserNames = (state) => state.user.userNames;

export const selectTeacherNames = (state) => state.user.teacherNames;
