const initialState = {
  description: null,
  explanation: null,
  userId: null,
  testCases: [],
};

export default (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
